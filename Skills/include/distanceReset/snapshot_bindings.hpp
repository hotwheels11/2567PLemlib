#pragma once
#include "distanceReset/snapshot_pose.hpp"
/*
namespace snapshot_pose {

// Forward declaration of Quadrant so this header doesn't require the full enum definition.
enum class Quadrant;

// One-call API for auton.
// If ok==false, pose is not modified.
// If ok==true, pose is applied via odom.set_position(...).
SnapshotResult snapshot_setpose_quadrant(Quadrant q);

} // namespace snapshot_pose*/